[project]
name = "mysql-python-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mysql-connector-python>=26.7.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
mysql-python-practice = "mysql_python_practice:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
